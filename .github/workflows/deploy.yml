name: Deploy to AWS S3 + CloudFront

on:
  push:
    branches:
      - main  # Deploy when pushing to main branch
  workflow_dispatch:  # Allow manual trigger

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive  # Fetch submodules (_data, avatars)
          fetch-depth: 0  # Full history for Jekyll
      
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '2.7'  # Compatible with old gem versions
          bundler-cache: false  # Disabled - no Gemfile.lock
      
      - name: Install dependencies
        run: |
          gem install bundler -v 2.4.22
          bundle install --jobs 4 --retry 3
      
      - name: Build Jekyll site
        run: |
          bundle exec jekyll build --verbose
        env:
          JEKYLL_ENV: production
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-west-1
      
      - name: Sync to S3 (subfolder)
        run: |
          aws s3 sync _site/ s3://${{ secrets.S3_BUCKET }}/${{ github.event.repository.name }}/ \
            --delete \
            --cache-control "public, max-age=300" \
            --exclude ".git/*" \
            --exclude ".github/*"
      
      - name: Sync to S3 root (home page)
        run: |
          echo "Deploying xrdocs.github.io to S3 root so CloudFront root URL works"
          aws s3 sync _site/ s3://${{ secrets.S3_BUCKET }}/ \
            --cache-control "public, max-age=300" \
            --exclude ".git/*" \
            --exclude ".github/*" \
            --exclude "telemetry/*" \
            --exclude "programmability/*" \
            --exclude "application-hosting/*" \
            --exclude "ncs5500/*" \
            --exclude "asr9k/*" \
            --exclude "8000/*" \
            --exclude "automation/*" \
            --exclude "cnbng/*" \
            --exclude "design/*" \
            --exclude "multicast/*" \
            --exclude "packet-fronthaul/*" \
            --exclude "routed-pon/*" \
            --exclude "security/*" \
            --exclude "cisco-service-layer/*" \
            --exclude "SONiC/*" \
            --exclude "cloud-scale-networking/*" \
            --exclude "virtual-routing/*" \
            --exclude "ztp/*" \
            --exclude "xrdocs.github.io/*"
      
      - name: Invalidate CloudFront cache (subfolder)
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ secrets.CF_DIST_ID }} \
            --paths "/${{ github.event.repository.name }}/*"
      
      - name: Invalidate CloudFront cache (root)
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ secrets.CF_DIST_ID }} \
            --paths "/*"
      
      - name: Deployment summary
        run: |
          echo "✅ Deployment complete!"
          echo "Repository: ${{ github.event.repository.name }}"
          echo "S3 Path: s3://${{ secrets.S3_BUCKET }}/${{ github.event.repository.name }}/"
          echo "CloudFront URL: https://d101lr0mb7xh5s.cloudfront.net/${{ github.event.repository.name }}/"

